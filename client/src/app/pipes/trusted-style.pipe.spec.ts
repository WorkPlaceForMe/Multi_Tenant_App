import { TrustedStylePipe } from './trusted-style.pipe';

describe('TrustedStylePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustedStylePipe();
    expect(pipe).toBeTruthy();
  });
});
